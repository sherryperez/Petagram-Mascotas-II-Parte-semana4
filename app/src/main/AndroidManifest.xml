<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.overne.petagram">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.overne.petagram.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.overne.petagram.About"
            android:parentActivityName="com.overne.petagram.MainActivity"
            >

            <!-- soporte versiones anteriores a andorid 4 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.overne.petagram.MainActivity"></meta-data>
        </activity>

        <activity android:name="com.overne.petagram.Detalle"
            android:parentActivityName="com.overne.petagram.MainActivity"
            >

            <!-- soporte versiones anteriores a andorid 4 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.overne.petagram.MainActivity"></meta-data>
        </activity>

        <activity android:name="com.overne.petagram.Contacto"
            android:parentActivityName="com.overne.petagram.MainActivity"
            >
            <!-- soporte versiones anteriores a andorid 4 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.overne.petagram.MainActivity"></meta-data>
        </activity>
    </application>

</manifest>
